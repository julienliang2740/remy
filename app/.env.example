# App-local AI provider.
# First pass supports mock only. Leave unset or keep this value for local app work.
EXPO_PUBLIC_REMY_AI_PROVIDER=mock

# Real provider clients and secrets are intentionally out of scope for this
# frontend-only pass. Add a backend/key broker before introducing provider keys.
